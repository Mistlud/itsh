# 이 곡 노래방에 있어? — 기획서 (Plan)

> "이 곡 노래방에 있어?" (itsh)  
> 곡명 또는 가수명을 검색하면, 해당 곡이 금영·TJ 등 노래방 기기에 수록되어 있는지 즉시 확인해주는 1페이지 반응형 웹앱.

---

## 1. 서비스 개요

- **목적**: 특정 곡이 노래방에 수록되어 있는지, 어느 브랜드(금영/TJ 등)에 몇 번으로 등록되어 있는지 빠르게 확인한다
- **형태**: 1페이지 웹 애플리케이션 (반응형)
- **대상 사용자**: 불특정 다수 (공개 배포)
- **배포 환경**: Vercel
- **상태**: v1.0 구현 완료, 빌드 성공

---

## 2. 핵심 기능 요구사항

### 2-1. 곡 검색

- [x] 검색창에 **곡명** 또는 **가수명** 키워드 입력 후 검색
- [x] 검색 방식 선택: **곡명 검색** / **가수명 검색** — **탭 형식** (곡명 | 가수명)
- [x] 입력 후 1초 경과 시 자동 검색 (버튼 없이도 동작, 버튼도 제공)
- [x] 검색 결과가 없을 경우: "해당 곡이 노래방에 없거나, 검색어를 확인해 주세요." 메시지 표시

### 2-2. 검색 결과 표시

- [x] 결과 목록에서 각 항목마다 표시할 정보:
  - 브랜드 뱃지 (금영 / TJ / DAM / JOYSOUND 등)
  - 곡 번호 (no)
  - 곡명 (title)
  - 가수명 (singer)
  - 출시일 (release)
- [x] 동일 곡이 여러 브랜드에 수록된 경우 각각 별도 항목으로 표시
- [x] 브랜드별 색상 뱃지로 시각적 구분 (금영=파랑, TJ=빨강, DAM=초록, JOYSOUND=주황, UGA=보라)

### 2-3. TJ 일본곡 필터 버튼

- [x] **"TJ 일본곡"** 토글 버튼 1개 (정렬 드롭다운 옆에 배치)
  - OFF (기본): 전체 결과 표시
  - ON: `brand === 'tj'` 이고 곡 번호가 아래 일본 구간인 항목만 표시
    - `6100 ~ 6999`
    - `25000 ~ 29000`
    - `52565 ~ 67999`
    - `68000 ~ 68999`
- [x] 이 판별 로직은 `utils/karaoke.js`의 `isTjJapanese(no)` 함수로 구현

### 2-4. 검색 결과 정렬

- [x] 정렬 기준 선택 — **드롭다운** 방식:
  - 최신순 (release 내림차순) — **기본값**
  - 오래된 순 (release 오름차순)
  - 곡명 가나다순 (title 오름차순)
  - 곡 번호순 (no 오름차순)

---

## 3. 비기능 요구사항

- [x] 반응형 레이아웃 (모바일 대응, 720px 기준)
- [x] 로딩 상태 표시: 스피너 + "검색 중..." 안내 텍스트
- [x] 에러 처리:
  - API 호출 실패 시: "일시적 오류입니다. 잠시 후 다시 시도해 주세요."
  - 검색 결과 없음: "해당 곡이 노래방에 없거나, 검색어를 확인해 주세요."
- [x] 초기 화면: **타이틀 + 검색창** (탭 포함) 만 표시 — 검색 전 결과 영역 없음

---

## 4. 기술 스택

| 영역 | 기술 |
|------|------|
| 프론트엔드 | React 19 + Vite 8 + Vanilla CSS |
| API | Manana API (`https://api.manana.kr`) |
| 정렬/필터 | 프론트엔드 자체 처리 |
| 배포 | Vercel |

> **서버리스 함수 불필요**: Manana API는 인증키 없이 클라이언트에서 직접 호출 가능 (CORS 허용, 무료, 호출 제한 없음).  
> wsc2h와 달리 `/api/*` 서버리스 래퍼가 필요 없어 구조가 더 단순하다.

---

## 5. API 사용 계획

### 5-1. 사용 엔드포인트

| 기능 | 엔드포인트 |
|------|-----------| 
| 곡명 검색 | `GET https://api.manana.kr/karaoke/song/{곡명}.json` |
| 가수명 검색 | `GET https://api.manana.kr/karaoke/singer/{가수명}.json` |

### 5-2. 응답 데이터 구조

```json
[
  {
    "brand": "tj",
    "no": "12345",
    "title": "곡명",
    "singer": "가수명",
    "composer": "작곡가",
    "lyricist": "작사가",
    "release": "2026-08-04"
  }
]
```

### 5-3. 브랜드 표기 매핑

| API 값 | 표시명 | 색상 |
|--------|--------|------|
| `kumyoung` | 금영 | #3b82f6 (파랑) |
| `tj` | TJ | #ef4444 (빨강) |
| `dam` | DAM | #22c55e (초록) |
| `joysound` | JOYSOUND | #f59e0b (주황) |
| `uga` | UGA | #8b5cf6 (보라) |

---

## 6. 프로젝트 구조

```
itsh/
├── src/
│   ├── App.jsx               # 메인 컴포넌트 (검색 상태, 정렬/필터 로직)
│   ├── main.jsx              # React 진입
│   ├── index.css             # 전역 스타일 + 반응형
│   ├── components/
│   │   ├── SearchBar.jsx     # 타이틀 + 검색창 + 탭 (곡명|가수명)
│   │   ├── ControlBar.jsx    # 정렬 드롭다운 + TJ 일본곡 토글 버튼
│   │   └── ResultList.jsx    # 결과 목록 + 각 카드 (브랜드 뱃지 포함)
│   └── utils/
│       ├── api.js            # fetchBySong, fetchBySinger 함수
│       └── karaoke.js        # isTjJapanese(no) — TJ 일본곡 번호 판별 + BRAND_INFO
├── index.html
├── package.json
├── vite.config.js
├── Plan.md
├── README.md
└── progress.md
```

---

## 7. 확정 사항

| # | 항목 | 결정 |
|---|------|------|
| 1 | **초기 화면** | 타이틀 + 검색창(탭 포함)만 표시 |
| 2 | **검색 방식 UI** | 탭 형식 (곡명 \| 가수명) |
| 3 | **필터** | "TJ 일본곡" 토글 버튼 1개 (정렬 드롭다운 옆) |
| 4 | **정렬 UI** | 드롭다운 (최신순 기본) |
| 5 | **Vercel 배포 도메인** | `itsh.vercel.app` |
| 6 | **TJ 일본곡 판별 구간** | 6100~6999 / 25000~29000 / 52565~67999 / 68000~68999 |

> 판별 구간은 실제 Manana API 데이터로 검증 완료 (해당 구간에 일본곡 또는 공란만 존재함을 확인)

---

## 8. 남은 작업

1. [x] 컨펌 완료
2. [x] React + Vite 프로젝트 셋업 (빌드 성공, 21 modules)
3. [x] Manana API 연동 (클라이언트 직접 호출, 서버리스 불필요)
4. [x] 검색 UI 구현 (SearchBar — 탭 + 검색창 + 자동검색 debounce)
5. [x] 결과 목록 UI 구현 (ResultList — 카드, 로딩/에러/빈 상태)
6. [x] 필터 + 정렬 기능 구현 (ControlBar — TJ 일본곡 버튼 + 드롭다운)
7. [x] 반응형 CSS 적용 (720px 기준)
8. [x] 로딩/에러 UI 적용
9. [ ] Vercel 배포
